(() =>{

	// self-invoking anon function (or Lambda)
	// start of every js file (() =>{          })()
	// var => means create a variable
	// whatever comes after the = is what the variable ref
	

	var images = document.querySelectorAll('img');

	//document is always the webpage
	// after the dot is always a method or a property

	console.log(images);

})()